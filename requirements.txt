# ─── Core инфраструктура (от организаторов) ───
fastapi==0.115.6
httpx==0.27.2
pydantic==2.10.4
pytest==8.3.4
tenacity==9.0.0
uvicorn==0.34.0

# ─── ML стек ───
lightgbm>=4.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyarrow>=14.0   # parquet engine

# ─── Биржевые данные ───
aiomoex>=2.1    # MOEX ISS публичный API (для свечей)
aiohttp>=3.9    # требуется aiomoex и async LLM-агентам

# ─── Config / SSL ───
pyyaml>=6.0
certifi>=2024.2  # фикс macOS CA chain

# ─── Новости (RSS keyless источники) ───
feedparser>=6.0
lxml>=4.9
beautifulsoup4>=4.12
requests>=2.31

# ─── Telegram (опционально, для российских ТГК) ───
# Раскомментируй когда будет .session файл
# telethon>=1.30
